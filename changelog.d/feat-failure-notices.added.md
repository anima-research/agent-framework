- Opt-in `failureNotices`: when a turn fails outright (provider error,
  refusal, over-budget), post a short automatic notice to the agent's locus on
  the first failure of a streak and every 5th after, so people can tell a
  dead turn from a quiet one.
