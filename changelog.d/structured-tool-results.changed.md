- `code_execution` now receives JSON from a tool's `structuredContent` when provided; direct model calls keep the readable content. Scripts parsing the rendered text of such tools should instead parse the structured JSON with `json.loads`. Tools without structured results retain their existing serialization. Oversized structured results return an explicit error at the existing 5,000,000-character script limit instead of incomplete JSON.
